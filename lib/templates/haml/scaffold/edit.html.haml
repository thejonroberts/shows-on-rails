- content_for(:title) { "#{@<%= singular_table_name %>.name}: Edit Profile" }
- content_for(:meta_description) { "Edit information for #{@<%= singular_table_name %>.name}" }

%h1 Editing <%= singular_table_name %>

= render 'form'
