- content_for(:title) { "Create a <%= singular_table_name.titleize %>" }
- content_for(:meta_description) { "Create a new <%= singular_table_name %>." }

%h2 New <%= singular_table_name %>

= render 'form'
