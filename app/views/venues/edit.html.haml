- content_for(:title) { "#{@venue.name}: Edit Profile" }
- content_for(:meta_description) { "Edit information for #{@venue.name}" }

%h1 Editing venue

= render 'form'

%button
  = link_to 'Show', @venue
%button
  = link_to 'Back', venues_path
