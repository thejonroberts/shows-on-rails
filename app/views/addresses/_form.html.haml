= form_for @address do |f|
  - if @address.errors.any?
    #error_explanation
      %h2= "#{pluralize(@address.errors.count, "error")} prohibited this address from being saved:"
      %ul
        - @address.errors.full_messages.each do |message|
          %li= message

  -# TODO: This must be sent with user / venue - not optional, skipping test for now.
  -# .field
  -#   = f.label :addressable
  -#   = f.text_field :addressable
  .field
    = f.label :line_one
    = f.text_field :line_one
  .field
    = f.label :line_two
    = f.text_field :line_two
  .field
    = f.label :city
    = f.text_field :city
  .field
    = f.label :state
    = f.text_field :state
  .field
    = f.label :country
    = f.text_field :country
  .field
    = f.label :country_code
    = f.text_field :country_code
  .field
    = f.label :zip_code
    = f.text_field :zip_code
  .actions
    = f.submit 'Save'
